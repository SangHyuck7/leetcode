/**
 * @param {number} n
 * @return {boolean}
 */
//풀이참고
//n = 4 이상의 수에 대해서 (n-2)진수로 표현하면 항상 n이 “12”와 같은 형태가 되어 회문이 될 수 없기 때문에 항상 false
const isStrictlyPalindromic = function(n) {
  
  return false;
};